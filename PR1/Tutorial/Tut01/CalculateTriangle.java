package PR1Tutorial.Tut01;

public class CalculateTriangle {
    public static void main(String[] args) {
        double base = 3;
        double height = 1.5;
        System.out.println("The triangle's base is " + base + " (cm) and height is " + height + " (cm).\nIt\'s area is " + (base * height / 2) + " (cm2).");
    }
}